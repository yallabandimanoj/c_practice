#include <stdio.h>
#include<string.h>

int main() {
    
    char str='c';

   // scanf("%c",str);


    //printf("The string is:%s %c\n",str, str[3]);
    //printf("The string length is: %ld\n",strlen(str));
    printf("%c",str);
    return 0;
}
